<div class="mt-4">
    <h3>Sửa Khóa Học</h3>
    {{!-- vì html chỉ có GET và POST, nên cần dùng thư viện method-override --}}
    {{!-- Ở đây form vẫn submit với methob POST nhưng middleware (method-override) sẽ biến nó thành PUT ở server --}}
    <form method="POST" action="/courses/{{course._id}}?_method=PUT">
        <div class="form-group mt-4">
            <label for="name">Tên khóa học</label>
            <input type="text" class="form-control" value="{{course.name}}" id="name" name="name">  
        </div>

        <div class="form-group">
            <label for="description">Mô tả</label>
            <textarea class="form-control" id="description" name="description">{{course.description}}</textarea>
        </div>

        <div class="form-group">
            <label for="videoId">VideoID</label>
            <input type="text" class="form-control" value="{{course.videoId}}" id="videoId" name="videoId">  
        </div>

        <div class="form-group">
            <label for="level">Mức độ</label>
            <input type="text" class="form-control" value="{{course.level}}" id="level" name="level">  
        </div>

        <button type="submit" class="btn btn-primary">Lưu lại</button>
    </form>
</div>